import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
  paperCalc: {
    margin: "6px",
    padding: "16px",
    height: "100%",
  },
  paperCalcMob: {
    margin: "6px",
    padding: "16px",
    marginTop: "32px",
    height: "100%",
  },
}));

export default useStyles;
